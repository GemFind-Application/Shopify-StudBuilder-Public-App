import { CONST } from "./black-box/constants";
const { TEST_BAR } = CONST;
export const config = {
    [TEST_BAR]: {
        target: "#MainContent,.main-content",
        className: "stud-builder-advance-test-q78erabc",
        styles: {
            width: "100%",
        },
    },
};
